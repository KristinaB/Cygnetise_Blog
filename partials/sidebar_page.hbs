{{!

	SIDEBAR - PAGE
	- Configuration of this file is optional.

}}

	<div class="sidebar">
		<div class="widget blog_info">
			{{#if @blog.logo}}
			<a href="{{@blog.url}}"><img src="{{@blog.logo}}" class="logo" alt="{{@blog.title}}"></a>
			{{else}}
			<h3><a href="{{@blog.url}}">{{@blog.title}}</a></h3>
			{{/if}}
			<p>{{@blog.description}}</p>
		</div>

		{{! ADD, REMOVE OR MOVE THE WIDGET TAGS BELOW TO CONFIGURE THE SIDEBAR AREA }}
		{{> widget_share}}
		{{> widget_copyright}}

	</div>
